singularity build --fakeroot --sandbox ubuntu20.04 generate.def

# custom configuration for emacs
mkdir -p home
mkdir -p home/.emacs.d
cp ./config/emacs/init.el home/.emacs.d/init.el

# bashrc for aliases
cp ./config/bash/bashrc home/.bashrc
